{
  "name": "rev-hunter-ai-monorepo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && npx rimraf public && npx shx mv frontend/dist public",
    "install": "cd frontend && npm install"
  }
}
